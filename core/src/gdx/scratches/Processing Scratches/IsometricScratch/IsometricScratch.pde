
/*int TILE_SIZE = 8;
 int nIsoX, nIsoY;
 
 void setup() {
 size(400, 400);
 }
 
 void draw() {
 background(255);
 translate(width / 2, height / 4);
 fill(0);  
 for (int y = 0; y < width/TILE_SIZE; y+=TILE_SIZE) {
 for (int x = 0; x < height/TILE_SIZE; x+=TILE_SIZE) {
 nIsoX = (x - y) / 2;
 nIsoY = (x + y) / 4;
 ellipse (nIsoX * TILE_SIZE, nIsoY * TILE_SIZE, TILE_SIZE, TILE_SIZE);
 }
 }
 }
 */